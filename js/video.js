var videoUrls = ["//www.cunshao.com/666666/api/web.php?video=1", "//www.cunshao.com/666666/api/web.php?video=2", "//www.cunshao.com/666666/api/web.php?video=3"]; var currentVideoIndex = 0; var videoPlayers = document.getElementsByClassName('videoPlayer'); function togglePlayPause() { for (var i = 0; i < videoPlayers.length; i++) { var videoPlayer = videoPlayers[i]; if (videoPlayer.paused) { videoPlayer.play() } else { videoPlayer.pause() } } } function playNextVideo() { for (var i = 0; i < videoPlayers.length; i++) { var videoPlayer = videoPlayers[i]; if (currentVideoIndex < videoUrls.length - 1) { currentVideoIndex++; videoPlayer.src = videoUrls[currentVideoIndex]; videoPlayer.load(); videoPlayer.play(); return currentVideoIndex = 0 } } } for (var i = 0; i < videoPlayers.length; i++) { videoPlayers[i].src = videoUrls[currentVideoIndex]; videoPlayers[i].load() }